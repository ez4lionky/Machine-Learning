Q5:
a. 训练100w后，模型收敛，最终结果表明，MPG = w * Temperature + b  (w约等于0.32527938632279557，b约等于40.3569438364434)
b. 使用上述线性回归模型去预测2018年的数据，mean absolute error约等于1.80，在训练次数较多的情况下，模型可以较好地拟合，但若只迭代1000次，MAE约等于16.95，误差较大
c. 使用2018年的数据进行训练，预测2017年的数据，迭代1000次时模型误差略大于b中的结果，但随着迭代次数的增加，误差逐渐较小，当迭代100w次时，MAE约等于1.57，小于b中的误差

Q6:
使用2017年的数据进行训练，预测2018年的结果：添加log项系数后，模型收敛速度更快，大概只需要10w次迭代即可收敛，并且最后的MAE值略小于Q5中简单的线性回归模型，迭代50w与100w次的MAE值近似相等，为1.79，但缺点在于模型训练时间更久
使用2018年的数据预测2017年，模型同样收敛速度更快，并且最后MAE值更小，迭代50w次MAE结果为1.53，迭代100w次MAE结果为1.49.
